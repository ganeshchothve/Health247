<nav class="navbar navbar-expand-lg navbar-light bg-light justify-content-between">
  <%= link_to 'Home', user_signed_in? ? user_path(current_user.id) : root_path, class: "navbar-brand btn btn-secondary text-light"%>
    <ul class="navbar-nav mr-auto">
    <li class="nav-item">
      <div>Health247</div>
    </li>
  </ul>
  <div id="navbarSupportedContent" class="float-end">
    <ul class="navbar-nav mr-auto">
    <% if !user_signed_in? %>
      <li class="nav-item active">
        <div class="dropdown">
          <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Register
          </button>
          <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
            <%= link_to 'Doctor', new_user_registration_path(user: {role: 'doctor'}), class: 'dropdown-item' %>
            <%= link_to 'Patient', new_user_registration_path(user: {role: 'patient'}), class: 'dropdown-item' %>
          </div>
        </div>
      </li>
    <% end %>
      <li class="nav-item">
      <% if user_signed_in? && current_user.role == 'patient' %>
        <%= link_to 'My Appointments', user_appointments_path(current_user.id), class: 'ml-2 btn btn-secondary' %>
      <% end %>
      <% if user_signed_in? %>
        <%= link_to 'My profile', profile_user_path(current_user.id), class: 'ml-2 btn btn-secondary' %>
        <%= link_to 'Log Out', destroy_user_session_path, method: :delete, class: 'ml-2 btn btn-secondary' %>
      <% else %>
        <%= link_to 'Login', new_user_session_path, class: 'ml-2 btn btn-secondary' %>
      <% end %>
      </li>
      </div>
    </ul>
  </div>
</nav>